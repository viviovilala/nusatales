import apiClient, {
    clearStoredToken,
    getStoredToken,
    setStoredToken,
} from "./apiClient";

export {
    clearStoredToken,
    getStoredToken,
    setStoredToken,
};

export default apiClient;
